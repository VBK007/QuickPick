<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.quickpick">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Quickpick"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".RequestDriveractivity"
            android:label="@string/title_activity_request_driveractivity"></activity>
        <activity
            android:name=".Maps.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".HomeLayoutforDrivers.homefordriandowner"
            android:theme="@style/Mytheme2" />
        <activity
            android:name=".EndUserLayouts.HomeENdUser"
            android:theme="@style/Mytheme2" />
        <activity
            android:name=".loginactivity"
            android:theme="@style/Mytheme2" />
        <activity
            android:name=".register"
            android:theme="@style/Mytheme" />
        <activity
            android:name=".mainactivity"
            android:theme="@style/Mythmain" />
        <activity
            android:name=".Admin.Adminactivity"
            android:theme="@style/Mytheme2" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/MythemeS">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <service android:name=".Services.MyFirebaseMessagingSertives">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>